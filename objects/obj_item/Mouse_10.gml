///@description TOca a animação quando o mouse entra no obj

// Checa se o obj não está bloqueado para tocar a animação
if (bloqueado == false)
{
	// Define a velocidade da animação em 1
	image_speed = 1;
}